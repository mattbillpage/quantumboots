package com.example.quantumboots;

import net.neoforged.neoforge.common.ModConfigSpec;

public class Config {
    //private static final ModConfigSpec.Builder BUILDER = new ModConfigSpec.Builder();
        // nothing to config for the mod rn, likely add settings here if i end up implementing seasons part of the mod but i doubt i will given that serene seasons already exists

}